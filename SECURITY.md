# 출결메이트 보안 기준 (SECURITY.md)

이 문서는 출결메이트의 **필수 보안 기준**이다.  
`main` 병합 전에 보안 관리자(보안 가온)가 이 문서의 체크리스트를 통과시켜야 한다.

출결·성명·출석번호·결석 사유는 학생 개인정보이며, 학교생활기록의 일부다.  
법률로 정한 경우 외에는 학생(미성년이면 보호자 포함) 동의 없이 제3자에게 제공할 수 없다.  
이 프로젝트는 교육부·KERIS 공식 프로그램이 아니므로, **나이스 밖의 서버로 실명 출결을 올리지 않는 것**이 가장 싼 준수 방법이다.

관련 근거(현장 적용 시 최신 조문 재확인):

- 「개인정보 보호법」 및 「개인정보의 안전성 확보조치 기준」(개인정보보호위원회 고시)
- 「교육기본법」 제23조의3, 「초·중등교육법」 제25조·제30조의4·제30조의6
- 학교생활기록 작성 및 관리지침(출결상황 포함)
- OWASP ASVS 5.x / Cheat Sheet (XSS, 주입, 프론트 저장, 설정)
- Chrome Extension Manifest V3 권한·CSP 가이드

---

## 0. 한 줄 원칙

1. **인증·인가는 서버가 한다.** 브라우저 `if` 문과 `localStorage` 역할 값은 권한이 아니다.
2. **입력은 코드일 수 있다.** 화면에는 글자로, DB/쿼리에는 파라미터로.
3. **비밀은 프론트에 없다.** 서비스 롤 키, AI 키, DB 비밀번호는 브라우저·확장 번들에 넣지 않는다.
4. **출결 실데이터는 기기 안에 둔다.** 구글은 신원만. 우리 DB에 실명 평문 금지. 동기화는 기본 꺼짐.
5. **나이스 일일출결 창은 확장이 직접 조작한다.** 마감 칸·팝업·P칸·저장 포함. ‘창을 건드리지 않는다’·‘제스처 후에만’은 폐기. 로그인·인증서만 교사.
6. **의심되면 거부한다.** IDOR, 광역 권한, 숨은 fetch, `eval`, **GitHub에 올라간 실명 원본**은 Merge 사유가 되지 않는다.  
   현장 화면 검토 자체는 허용한다. 남는 산출물만 비식별이어야 한다.

로컬 전용 MVP라도 1~3번은 지킨다.  
“서버가 없으니 인증이 필요 없다”는 말이 **인가·노출·입력 불신까지 생략하는 핑계가 되면 안 된다.**

---

## 1. 출결메이트가 다루는 데이터

| 구분 | 예시 | 취급 |
| --- | --- | --- |
| 식별정보 | 이름, 학년·반·번호 | 기기 로컬만. GitHub·CI·원격 로그 금지 |
| 출결 | 구분(질병·미인정·기타·출석인정) × 종류(지각·조퇴·결석·결과), 칸, 사유 | 로컬만. 미리보기 후 나이스 화면에만 반영 |
| 사유 | 질병, 경조사, 체험학습, 기타 사유 문구 | 민감할 수 있음. 외부 전송 금지 |
| 교사 신원 | 구글 `sub`, 표시용 이메일 | 출결 칸을 나누는 열쇠. 출결 원문과 함께 우리 서버로 안 보냄 |
| 운영 정보 | 셀렉터, 앱 설정, 익명 오류 코드 | 식별정보 제거 후에만 수집 가능 |
| 절대 수집 금지 | 주민등록번호, 인증서, 나이스 비밀번호, 학부모 계정, 구글 비밀번호 | 코드·저장소·로컬 DB에도 넣지 않음 |

GitHub·CI·커밋·공개 이슈의 테스트·스크린샷·픽스처는 `학생01` 같은 가명만 사용한다.  
학교명+실반+날짜가 조합되면 식별 가능해질 수 있으므로 저장소에 올리지 않는다.

개발 대화에서 소유 교사가 실제 나이스 일일출결 화면을 보여 주는 것은 허용한다(15절).  
그때 얻은 지식은 **비식별 픽스처와 셀렉터 문서**로만 저장소에 반영한다.

브라우저 저장(IndexedDB, localStorage)은 **암호화된 서버 금고가 아니다.**  
같은 원본의 XSS 한 줄이면 읽힌다. 디스크에 남은 브라우저 프로필도 같은 PC의 다른 프로세스에 노출될 수 있다.  
따라서 “로컬이라 안전하다”가 아니라 **“우리 서버로 안 나간다 + XSS를 막는다 + 실기기 접근을 교사에게 알린다”**가 방어선이다.

---

## 2. 인증 — 누가 들어오나 · 서버가 확인

☐ 로그인(신원 확인)을 **서버가** 한다.  
☐ 브라우저 `if` 문으로 비밀번호를 비교하지 않는다.  
  → 비밀번호가 브라우저로 내려온 순간, 그 자체로 노출된 것이다.  
☐ 비밀번호는 평문·MD5·SHA-1로 저장하지 않는다. bcrypt / argon2 또는 검증된 Auth 서비스(Supabase Auth 등)의 기본 해시를 쓴다.  
☐ 세션은 HttpOnly + Secure + SameSite 쿠키 또는 헤더 토큰을 쓴다.  
  `localStorage`에 JWT를 두는 것은 XSS 한 번에 세션 탈취다.  
☐ 로그인 엔드포인트에 rate limit을 건다. 무차별 대입·자격 스터핑을 그대로 두면 AI 키·DB까지 열린다.

### 출결메이트 적용

- **구글 로그인(1차):** 신원은 구글에게 맡긴다. 우리 창에서 비밀번호를 받지 않는다. 상세는 `docs/auth.md`.
- 출결 원문은 구글·Firestore·우리 API로 올리지 않는다. IndexedDB를 `ownerSub`로 나눈다.
- “로그인 UI만 있고 DB는 하나”는 최악이다. 가온이 반려한다.
- Client Secret / refresh token을 프론트·확장 저장소에 두지 않는다. Access token은 `localStorage` 금지.
- 기기 잠금(선택)은 Web Crypto + 교사 암호 유도 키. 암호 자체는 저장하지 않는다.
- 나이스 로그인을 우리 코드가 대신하지 않는다. 인증서·SSO·VPN 자동화 금지.
- 구글 계정과 나이스 계정은 다르다. 구글 로그인으로 나이스에 들어가지 않는다.

---

## 3. 인가 — 무슨 권한 · 어느 데이터 · 서버가 매 요청

☐ “관리자다” 같은 역할을 **서버가** 정한다.  
☐ 브라우저에 저장된 값(`localStorage` · 쿠키 JS 변수)으로 역할을 판단하지 않는다.  
  → 누구나 스스로 권한을 가진다.  
☐ DB/API 요청마다 데이터를 제한한다.  
  Supabase RLS · Firebase Security Rules · 서버 쿼리 필터 + 인증.  
☐ 남의 `id`를 바꿔 보낸 요청도 막힌다.  
  URL·본문으로 남의 id를 바꿔 데이터에 접근하는 것이 IDOR. 기본은 거부.

### 출결메이트 적용

- 확장 ↔ 웹앱 메시지는 **우리 origin + 확장 ID**만 받는다. `onMessage`에서 sender를 검사하지 않으면 다른 확장·페이지가 대기열을 주입한다.
- 나이스 탭에 넣을 데이터는 교사가 미리보기에서 확인한 대기열뿐이다. 임의의 URL 파라미터로 전송을 시작하지 않는다.
- 다중 학급이 생기면 “요청에 적힌 학급 id”가 아니라 **현재 교사 세션이 소유한 학급**만 읽는다.
- 관리자 화면을 프론트 라우트만으로 숨기지 않는다. 숨긴 경로는 주소만 알면 열린다.

---

## 4. 입력 불신 — 입력은 코드일 수 있다

☐ 입력을 화면에 넣을 때 **글자로** 처리(이스케이프)한다.  
  React·Vue는 기본 제공. 직접 넣을 땐 `textContent` / `createTextNode`.  
  `innerHTML`, `document.write`, `eval`, `new Function`, `javascript:` URL 금지.  
☐ DB 쿼리는 값을 **파라미터로 바인딩**한다.  
  문자열로 잇지 않는다. SQL 인젝션 방지. ORM·쿼리빌더 기본 방어를 우회하지 않는다.  
☐ 클라이언트 검증은 UX용이다. 서버가 있으면 **반드시 재검증**한다.  
☐ 파일 업로드(엑셀·CSV)는 확장자만 믿지 않는다. 파싱 실패·수상한 매크로·과도한 행 수는 거부한다.  
☐ 사유·이름 필드에 HTML·수식을 넣어도 실행되지 않게 한다.  
  엑셀 재내보낼 때 셀이 `=` 로 시작하면 수식 주입(CSV injection)이 된다. 앞에 `'` 를 붙이거나 텍스트로 강제한다.

### 출결메이트 적용

- IndexedDB에서 꺼낸 값도 네트워크 입력과 같다. 다시 그릴 때 이스케이프한다.
- 나이스 DOM에 넣을 문자열은 텍스트 입력만. 페이지에 스크립트 태그를 삽입하지 않는다.
- 확장 content script는 페이지 JS와 스코프를 공유하지 않는다. 페이지의 `postMessage`를 그대로 신뢰하지 않는다.
- 휴일·구분·내용·교시 코드는 화이트리스트로만 받는다. 문서에 없는 조합 코드는 나이스에 넣지 않는다.

---

## 5. 노출 — 비밀키·비밀번호는 프론트 코드에 없다

☐ 비밀키·비밀번호가 코드/프론트엔드에 없다. → `.env`로 관리 (아래).  
☐ 관리자만 쓸 키가 브라우저에 없다.  
  예: Supabase `service_role`, Firebase Admin SDK → **서버 전용**.  
  브라우저에는 anon / 제한된 publishable 키만.  
☐ 배포 폴더에 `.sql` · `.env` · 백업 파일이 안 딸려간다.  
  빌드 산출물만 배포. 배포 후 `/.env`, `/backup.sql`, `/.git/` 주소로 검색해 404를 확인한다.

### 비밀 관리 (.env & git)

- 비밀(API 키·DB 비번·마스터키)은 `.env`에 두고 `.gitignore`에 `.env`를 등록한다. git에 올리지 않는다.
- 공유는 값 없이 이름만 담은 `.env.example`로 한다.
- AI API 키(OpenAI·Anthropic·Gemini·xAI)가 특히 위험하다.  
  유출되면 남이 내 크레딧으로 호출해 요금 폭탄이 된다. **사용량 한도(spending limit)** 를 걸어 둔다.
- 이미 올렸다면 git에서 지워도 소용없다. 클론·봇이 이미 가져갔다.  
  **유일한 방어는 키 폐기(rotate) 후 재발급**이다.
- pre-commit에 secret scan(gitleaks 등)을 넣는 것을 권장한다.
- GitHub Actions 비밀은 Repository secrets에만 둔다. 워크플로 로그에 echo 하지 않는다.

### 출결메이트 적용

- 웹앱·확장 소스에 API 키를 넣는 기능 자체를 1차에 두지 않는다.
- 나중에 AI 보조(사유 문장 다듬기 등)를 붙여도 **키가 브라우저에 있으면 안 된다.** 교사 자기 키를 로컬에만 두는 방식 또는 서버 프록시 + 한도.
- `service_role`로 RLS를 우회하는 관리자 페이지를 정적 호스팅에 올리지 않는다.

---

## 6. 바이브코딩에서 흔한 실수

아래는 그대로 재발하면 보안 리뷰가 **차단**하는 항목이다.

| 실수 | 왜 위험한가 | 출결메이트에서 |
| --- | --- | --- |
| 스토리지 버킷을 public으로 방치 | 업로드한 명단·이미지가 URL만 알면 열림 (Supabase/Firebase/S3 공통) | 출결 파일 버킷을 만들지 않거나, 인증된 교사만 + 목록 조회 금지 |
| 프로덕션 debug 모드 | 에러 화면에 스택·쿼리·경로 노출 | 프로덕션 소스맵·verbose 로그 끄기. 오류는 코드만 |
| 오래된 라이브러리 | AI가 옛 버전을 제안 → 알려진 취약점 | `npm audit` / Dependabot. 잠긴 버전을 리뷰 없이 올리지 않기 |
| rate limit 없음 | 무차별 로그인·API 비용 남용 | 서버가 생기는 순간 로그인·AI 프록시에 한도 |
| 클라이언트 검증만 믿음 | 개발자 도구로 우회 | 서버 재검증. 로컬만이라도 화이트리스트 + 미리보기 |
| 약한 비밀번호 저장 | DB 유출 시 그대로 읽힘 | Auth 서비스 기본값. 자체 해시 구현 금지 |
| CSRF | 악성 페이지가 “로그인된 나”로 요청 | 쿠키면 SameSite=Lax/Strict. 토큰은 헤더. 확장은 외부 사이트 폼이 우리 API를 못 치게 |
| `<all_urls>` 확장 권한 | 모든 사이트 DOM·쿠키에 손댐 | 나이스 호스트 + 웹앱 origin만 |
| 원격 스크립트 로드 | MV3에서 금지일 뿐 아니라 공급망 장악 | 확장·웹앱 번들에 코드를 포함. CDN JS 금지 |
| `eval` / 동적 `innerHTML` | XSS | 리뷰에서 검색어로 차단 |
| 실명 원본을 이슈·PR에 첨부 | 저장소가 제3자 제공이 됨 | GitHub에는 가명·블러·크롭만. 현장 원본은 저장소 밖 개발 대화에서만 |
| 분석 SDK 기본 삽입 | 출결 화면의 텍스트가 외부로 감 | GA/Mixpanel 등 기본 비활성. 넣더라도 식별 필드 마스킹 |

추가로 자주 놓치는 것:

- **의존성 혼란(dependency confusion):** 내부 패키지와 같은 이름을 공개 레지스트리에 올리면 CI가 악성 패키지를 받는다. 스코프를 고정한다.
- **프로토타입 오염:** `JSON.parse` 결과를 그대로 merge 하지 않는다.
- **오픈 리다이렉트:** `?next=` 로 외부 도메인에 보내 피싱.
- **클릭재킹:** 확장 팝업·설정 페이지에 `X-Frame-Options` / CSP `frame-ancestors 'none'`.
- **로그 과다:** `console.log(student)` 가 프로덕션에 남으면 교사 PC의 확장 도구에 실명이 쌓인다.

---

## 7. 브라우저 확장 (매크로) 전용 기준

나이스 자동입력은 확장의 `content_scripts`가 교육행정 화면에 손을 대는 일이다. 권한이 넓을수록 유출 면적이 커진다.

☐ Manifest V3만 사용한다.  
☐ `host_permissions` / `content_scripts.matches`는 필요한 나이스 도메인과 웹앱 origin으로 한정한다.  
  `*://*/*`, `<all_urls>` 금지.  
☐ 나이스 일일출결 창 직접 조작(마감 칸·팝업·P칸·저장)은 허용. ‘제스처 후에만’은 폐기. 로그인·인증서 자동화만 금지.  
☐ `tabs`, `cookies`, `webRequest`, `debugger`, `nativeMessaging`, `downloads`는 이유가 문서화되기 전에는 넣지 않는다.  
☐ 원격 코드 실행 금지. 확장 업데이트는 스토어/자체 패키지로만.  
☐ CSP: `script-src 'self'`. `unsafe-eval` 요청은 보안 리뷰 차단.  
☐ `web_accessible_resources`는 최소 파일 + 최소 matches. 열려 있으면 페이지가 확장 파일을 읽는다.  
☐ background service worker에서 출결 JSON을 우리 서버로 `fetch` 하지 않는다.  
☐ 메시지 채널: `chrome.runtime.sendMessage` sender `id` 검증. `externally_connectable`는 웹앱 origin만.  
☐ 페이지와 `postMessage`를 쓸 때도 `event.origin`을 검사한다.  
☐ 실패 로그에는 이름·번호를 넣지 않는다. `row=3 type=지각 result=ok` 정도.  
☐ 부분 실패를 성공으로 표시하지 않는다. 저장 버튼을 누른 뒤 나이스 응답을 확인하기 전에는 “반영됨”이 아니다.

나이스 화면 구조가 바뀌면 셀렉터만 고친다.  
셀렉터를 고친다고 권한을 넓히지 않는다.

---

## 8. 로컬 웹앱 · PWA · IndexedDB

☐ 기본 동작은 오프라인·로컬. 동기화 플래그 기본값 `false`.  
☐ IndexedDB 키 이름·DB 이름에 학교명·실명을 넣지 않는다.  
☐ XSS가 나면 IndexedDB는 통째로 읽힌다. 출력 인코딩이 로컬 앱의 서버 RLS다.  
☐ 민감 백업을 자동으로 클라우드 폴더에 떨어뜨리지 않는다. 보내기 전에 교사 확인.  
☐ PWA 캐시가 옛 JS를 붙잡고 보안 패치를 막지 않게 업데이트 전략을 둔다.  
☐ `postMessage` / `BroadcastChannel`로 확장과 말 때 origin 검사.  
☐ 화면 잠금이 없는 공용 교실 PC를 전제로 안내한다. 자리 이석 시 브라우저를 잠그라고 README에 적는다.

---

## 9. 공급망 · 빌드 · 배포

☐ lockfile을 커밋한다. `npm install`이 매번 최신을 가져오게 두지 않는다.  
☐ GitHub Actions는 `permissions: contents: read`를 기본으로 줄인다.  
☐ `pull_request_target` + 외부 PR 코드 실행을 섞지 않는다.  
☐ 배포 산출물에서 `.env`, `.map`(정책에 따라), `.sql`, `.git` 이 없는지 확인한다.  
☐ 써드파티 스크립트·iframe을 기본 UI에 넣지 않는다.  
☐ 라이선스·유지 여부가 불명한 “AI가 집어넣은” 패키지를 리뷰 없이 추가하지 않는다.

---

## 10. 로깅 · 오류 · 관측

☐ 프로덕션 로그에 이름, 번호, 사유, 토큰, 쿠키가 없다.  
☐ 스택 트레이스는 개발 환경에서만.  
☐ 자동입력 감사 로그는 교사 기기 로컬에만 두고, 원하면 마스킹된 요약만 보여 준다.  
☐ “익명 텔레메트리”라도 출결 화면 URL·DOM 텍스트를 보내지 않는다.

---

## 11. 법·운영상 주의 (학교 현장)

- 출결상황은 학교생활기록에 들어간다. 잘못된 자동입력은 정정대장 업무로 이어진다.
- 질병명 등 민감 정보는 특기사항·사유의 외부 공유를 특히 막는다.
- 학교·교육청 보안 지침, 망분리, 업무PC 반출 규칙과 충돌하면 **사용을 중단**한다.
- 교사 개인이 만든 보조 도구라도, 실명 명단을 **불특정 상용 AI·공개 이슈**에 붙여 넣는 것은 제3자 제공이 될 수 있다.
- 소유 교사와 이 프로젝트 개발 대화에서 화면 구조를 검토하는 것은 15절을 따른다. GitHub에는 실데이터를 올리지 않는다.
- 이 도구는 공식 나이스가 아니다. README 면책 문구를 삭제하는 PR은 차단한다.

이 문서는 법률 자문이 아니다. 학교 개인정보 보호책임자·교육청 기준이 더 엄하면 그쪽을 따른다.

---

## 12. PR / Merge 체크리스트 (보안 가온)

복사해서 PR에 붙인다. 하나라도 “아니오”면 `main` 불가.

```md
## 보안 리뷰

- [ ] 학생 실명·출결·사유가 **커밋·GitHub 이슈·PR·CI 로그**에 없다. 현장 원본은 저장소에 올리지 않았고, 필요하면 비식별 픽스처만 있다
- [ ] 기본 동작이 로컬 전용이다. 새 외부 전송이 있다면 목적·항목·보관이 이 문서에 적혀 있다
- [ ] 프론트/확장 번들에 비밀키·service_role·AI 키가 없다
- [ ] `.env` / `.sql` / 백업이 산출물에 없다
- [ ] 인증을 브라우저 if·localStorage 역할값으로 하지 않는다 (해당 시)
- [ ] 인가가 요청마다 서버/RLS에서 다시 적용된다 (해당 시). IDOR 테스트 흔적이 있다
- [ ] 출력은 textContent/프레임워크 이스케이프. innerHTML·eval 신규 사용 없음
- [ ] 쿼리는 파라미터 바인딩 (해당 시)
- [ ] 확장 권한이 최소다. <all_urls> 없음. matches가 나이스+웹앱로 한정
- [ ] 부분 실패를 성공으로 표시하지 않는다. 나이스 창 직접 조작(칸·팝업·저장)은 허용
- [ ] 메시지/postMessage sender·origin을 검사한다
- [ ] 의존성 잠금·명백한 취약 패키지가 없다
- [ ] debug·분석 SDK·public 버킷이 프로덕션 기본값이 아니다
- [ ] README 면책·비공식 고지가 유지된다

판정: 통과 / 수정 후 재검토 / 차단
근거:
요청 수정:
```

판정 정의:

- **통과:** 위 항목을 만족. Merge 가능.
- **수정 후 재검토:** 고칠 점이 있으나 설계를 뒤집지는 않음.
- **차단:** 실데이터 유출, 비밀 커밋, 광역 권한, 인증서 자동화, 미리보기 없는 저장. “나중에 고치자”로 통과시키지 않음.

---

## 13. 사고 대응

1. 유출이 의심되면 **배포를 멈추고** 키를 폐기(rotate)한다.
2. git 히스토리 rewrite만으로 키가 안전해지지 않는다고 본다.
3. 실명 데이터가 GitHub 이슈·Gist·커밋에 올라갔으면 삭제하고, 이미 복제됐을 가능성을 소유 교사에게 알린다.
4. 나이스에 잘못 저장된 출결은 이 앱이 “롤백”하지 않는다. 교사가 나이스에서 정정한다.
5. 사후 원인(커밋, 권한, 로그)을 이슈로 남기되 실데이터는 남기지 않는다.

---

## 15. 현장 나이스 화면 검토·시운전

확장이 실제 `[학급담임]-[출결관리]-[일일출결관리(담임용)]`에 붙으려면, 소유 교사가 연 화면을 보고 구조·시운전을 해야 한다.  
이것은 README 6.1과 같은 규칙이다.

### 허용

- 소유 교사가 직접 로그인한 화면을 **저장소 밖 개발 대화**에서 보여 주고 검토하는 것
- 이름·번호·사유를 가리거나 잘라 낸 이미지
- DOM 역할, 버튼 라벨, 그리드 축(날짜·교시·학생 행), 저장 후 메시지처럼 **구조만** 메모하는 것
- 교사 입회 시운전. 확장이 나이스 창을 직접 조작·저장해도 된다

### 금지

- 인증서, 비밀번호, 공동인증서 PIN, SSO 중간 화면을 첨부하거나 입력하라고 요청하는 것
- 실명 원본을 GitHub 이슈·PR·`tests/`·커밋에 올리는 것
- 봇·자동화가 나이스 로그인을 수행하는 것
- CI가 실나이스에 접속하는 것
- 시운전 결과를 앱이 나이스에서 되돌리는 기능 (롤백 없음)

### 저장소에 남는 것

- `학생01` 픽스처 HTML과 가명 CSV
- 라벨·역할 기준 셀렉터. 특정 학생 이름에 묶인 선택자는 쓰지 않는다
- PR에 적을 한 줄: `시운전: 확장 직접 조작 / 저장 여부`

### 시운전 순서

1. 웹앱 대기열을 확인한다.
2. 나이스 로그인·인증서만 교사.
3. 확장이 나이스 창을 직접 조작한다 (마감 칸·팝업·P칸·저장).
4. 저장해도 된다. 출결마감 버튼은 누르지 않는다.
5. 오입력은 나이스에서 정정. 앱 롤백 없음.

검토 중 실명이 대화에 보이더라도, 그 내용을 이슈 본문·커밋 메시지·셀렉터 주석에 복사하지 않는다.

---

## 16. 참고

- 개인정보보호위원회, 「개인정보의 안전성 확보조치 기준」 안내서
- 찾기쉬운 생활법령정보, 교육분야 개인정보보호
- 「초·중등교육법」 학생 자료 제공 제한
- Chrome for Developers, Improve extension security / Declare permissions (MV3)
- OWASP Cheat Sheet: XSS, HTML5 Storage, Injection, CSRF
- OWASP ASVS 5.x (프론트 보안, 인증, 인가, 설정)

문서 개정은 보안 관리자가 한다. 기능 봇이 편의 때문에 이 문서를 약화하는 PR은 차단한다.
