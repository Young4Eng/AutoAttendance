# 출결메이트 (Chulgyeol-Mate)

대한민국 중등학교 담임교사의 **나이스(NEIS) 일일출결 입력 부담**을 줄이기 위한 보조 웹앱 + 브라우저 자동화 도구입니다.

> 이 프로젝트는 교육부·KERIS 공식 프로그램이 **아닙니다**.  
> 나이스 화면의 최종 저장·확인 책임은 항상 담당 교사에게 있습니다.  
> 학생 개인정보는 외부 서버로 전송하지 않는 것을 1원칙으로 합니다.

---

## 1. 해결하려는 문제

4세대 나이스의 `[학급담임] - [출결관리] - [일일출결관리(담임용)]` 화면은 다음 이유로 입력이 느리고 실수가 많습니다.

- 조회 → 날짜 이동 → 학생·교시 셀 클릭 → 출결유형 선택 → 사유 입력 → 저장을 **매일·학생별로 반복**
- 팝업 **출결마감구분**: 구분(질병·미인정·기타·출석인정) × 종류(지각·조퇴·결석·결과). 기타는 사유 필요
- 그리드에 마감·조회·교시(과목)·종례·사유 열이 따로 있다. 교시 칸은 `미마감`으로 시작할 수 있음
- 주말·공휴일·수업일수가 아닌 날에 잘못 입력하면 중단·오류 발생
- 전입·편입·중도입학 학생은 개근·특기사항 처리 규칙이 다름
- 학부모 온라인 결석신고·체험학습 신청과 담임 화면을 대조해야 함
- 월말 마감, 출결 특기사항, 개근 일괄입력은 별도 점검이 필요

현장에서는 엑셀 출석부 → 나이스 복사·붙여넣기, 또는 크롬 확장 프로그램(내모 NAEMO 등)으로 우회하고 있습니다.  
출결메이트는 이 흐름을 **교사 친화 UI(웹앱) + 크롬 확장(MV3)** 으로 묶습니다. 유저스크립트는 쓰지 않습니다.

---

## 2. 제품 원칙

1. **로컬 우선**: 명단·출결·사유는 브라우저/기기 안에서만 처리. 구글은 신원(`sub`)만. 우리 DB에 실명 출결 평문 금지. 기기 간 동기화는 기본 꺼짐.
2. **미리보기 후 전송**: 자동입력 전 “무엇이 바뀔지”를 교사에게 보여 주고 확인받는다.
3. **중복 입력 방지**: 이미 반영된 건은 다시 넣지 않는다.
4. **규칙 검증**: 주말·공휴일, 기타 사유 공란, 문서에 없는 구분×종류 조합을 입력 전에 막는다.
5. **공식 시스템 존중**: 나이스 로그인·인증서·권한은 교사가 직접 수행. 우리는 UI 보조만 한다.
6. **투명성**: 자동입력이 어떤 셀을 어떤 값으로 바꾸는지 로그를 남긴다(학생 식별정보는 로컬만).

### 확정된 결정 (2026-09)

- 자동입력은 **크롬 확장 MV3**만. 유저스크립트 없음
- 구글 로그인은 **신원(`sub`)만**. 출결 원문은 IndexedDB의 `ownerSub` 칸. Firestore 평문 없음 (`docs/auth.md`)
- 나이스 팝업: 구분=질병·미인정·기타·출석인정, 종류=지각·조퇴·결석·결과
- 구분·종류 후 **교시 P 칸 한 번 클릭**. `/`는 나이스가 퍼뜨림. 칸마다 타이핑하지 않음
- 한 날짜가 끝나면 **저장**. 저장 없이 날짜를 바꾸면 입력 소실. **출결마감은 자동화하지 않음**
- 출석번호는 연속이 아님(전학 등 7 다음 9). 매칭은 번호+성명. 행 순번으로 메우지 않음
- 교사 PC 사용 시 Chrome+확장. Node/Git은 개발 PC만
- 봇: 결정·짧은 리뷰는 채널, 긴 코드는 1:1. 한 턴에 봇 하나. 기록은 GitHub

---

## 3. 권장 구조

```
출결메이트
├── web/                 # 교사 출결 정리 웹앱 (PWA 가능)
│   ├── 학급 명단 가져오기 (엑셀/CSV, 학년·반·번호·이름)
│   ├── 일일·기간 출결 입력 UI
│   ├── 구분×종류 선택 / 사유 템플릿
│   ├── 검증 엔진 (휴일, 필수사유, 교시 규칙)
│   ├── 전송 대기열 + 미리보기
│   └── 구글 로그인 (신원) + 계정별 IndexedDB 칸
├── extension/           # 크롬 확장 MV3만. 유저스크립트 없음
│   └── 나이스 일일출결관리(담임용) P칸 클릭 → 저장
├── SECURITY.md          # GitHub Security 탭용 (docs와 동일)
├── docs/                # 계약·봇·보안·운영
├── .github/             # PR·이슈 템플릿
└── tests/fixtures/      # 가명 샘플만
```

나이스 **쓰기용 공개 API는 없습니다.** 급식·시간표 등 Open API는 조회용입니다.  
따라서 나이스 반영은 브라우저 탭 간 로컬 자동화만 사용합니다.

---

## 4. MVP 범위 (1차)

### 웹앱
- [ ] 구글 계정 로그인. 기록은 `ownerSub` 칸에만 (`docs/auth.md`)
- [ ] 학급 1개 명단 등록 (엑셀 템플릿 제공)
- [ ] 날짜별 출결. 기본값은 출석(기록 없음)
- [ ] 구분: 질병 / 미인정 / 기타 / 출석인정
- [ ] 종류: 지각 / 조퇴 / 결석 / 결과 (`docs/data-contract.md`)
- [ ] 구분이 기타이면 사유 필수
- [ ] 기준 교시 P. 나이스에서 그 칸을 클릭하면 `/`가 퍼짐 (`docs/data-contract.md`)
  - 지각 + P 클릭: 조회(조례) + 1…P
  - 조퇴 + P 클릭: P…마지막 교시 + 종례
  - 결석 + P 클릭: 조회 + 전 교시 + 종례
  - 결과 + P 클릭: P만
  - 마감 열: `{구분}{종류}` (예: 질병지각)
- [ ] 여러 날짜 일괄 작성
- [ ] “나이스 전송 대기” 목록과 미리보기

### 매크로/확장
- [ ] `*.neis.go.kr` 일일출결관리(담임용) 화면 감지
- [ ] 학년도·학년·반·일자 일치 확인 후 그리드 매칭
- [ ] 학생 매칭 (출석번호+성명). 중간 번호 공백(7 다음 9)을 행 순번으로 메우지 않음. 실패 시 중단
- [ ] 출결마감구분 팝업 또는 대상 칸에 구분·종류·사유 입력
- [ ] 한 날짜 입력이 끝나면 **저장**. 저장 없이 날짜를 바꾸지 않음
- [ ] 출결마감 버튼은 자동 클릭하지 않음
- [ ] 성공 시 웹앱 쪽 “반영됨” 표시
- [ ] 주말·공휴일·저장 실패 시 즉시 중단

### 명시적 비범위 (1차에서 하지 않음)
- 생기부 세특·행발 자동입력
- 나이스 서버 직접 호출 / 인증서 우회
- 학생·학부모 계정 연동
- 프로젝트 소유 DB(Firestore 등)에 실명 출결 평문 저장
- 구글 Drive/Gmail 광역 권한, 기기 간 평문 동기화 (2차는 암호문+앱폴더만 검토)
- 유저스크립트(Tampermonkey 등)

---

## 5. 이후 단계 (2차 이후)

- 월별 마감 점검 체크리스트
- 출결 특기사항(장기결석·기타결석) 초안 보조 (자동 확정 금지)
- 전입·편입 학생 예외 플래그
- 학부모 온라인 결석신고와 대조하는 수동 체크 UI
- 오프라인 PWA, 암호화 로컬 백업
- 나이스 화면 개편 시 DOM 셀렉터 핫픽스 채널

---

## 6. 보안·개인정보

출결 데이터는 학생 개인정보이며 학교생활기록의 일부입니다. 자세한 기준은 `docs/SECURITY.md`입니다. PR은 `.github/PULL_REQUEST_TEMPLATE.md` 체크리스트를 채운 뒤, 보안 관리자 승인 없이 `main`에 넣지 않습니다.

- **로컬 우선**: 이름·번호·출결·사유를 우리 서버·분석 SDK·상용 AI에 보내지 않음
- **구글은 신원만**: 로그인 후 IndexedDB를 계정별로 나눔. 출결 원문은 구글에 안 올림 (`docs/auth.md`)
- **가짜 로그인 금지**: 브라우저 `if`/프론트 비밀번호 비교 금지. 구글이 준 `sub`만 칸 열쇠
- **IndexedDB는 금고가 아님**: XSS·같은 PC 접근에 노출될 수 있음. 출력은 글자(이스케이프)로만
- **확장 권한 최소**: Manifest V3, 나이스 호스트 + 웹앱 origin만. `<all_urls>` 금지
- **미리보기 없는 나이스 저장 금지**. 부분 실패를 성공으로 표시하지 않음
- **비밀키·service_role·AI 키는 프론트/확장 번들에 없음**
- **GitHub·CI·커밋에는 가명만** (`학생01`). 실명·학교명 원본 캡처를 저장소에 올리지 않음

현장 화면을 보고 시운전하는 절차는 아래 6.1과 `docs/SECURITY.md` 15절을 따릅니다.

### 6.1 실제 나이스 화면 검토·시운전

확장이 동작하려면 **교사가 연 실제 일일출결관리(담임용) 화면**을 보고 DOM·버튼 순서·저장 반응을 확인해야 합니다. 이것은 보안 원칙과 모순되지 않습니다. **실데이터는 저장소에 남기지 않고, 구조만 남깁니다.**

허용:

- 소유 교사가 로그인한 화면을 개발 대화에서 보여 주고, 검토·셀렉터 확인·시운전을 함께 진행
- 이름·번호·사유를 가리거나 자른 이미지, 또는 구조만 적은 메모
- 교사 입회 아래 미리보기 → (합의된 경우에만) 저장. 저장 후 나이스 화면을 육안으로 재확인

금지:

- 인증서, 비밀번호, SSO 화면, 주민번호를 대화·이슈·커밋에 첨부
- 실명 명단·원본 캡처를 GitHub 이슈/PR/`tests/`에 커밋
- 봇이 나이스 로그인을 대신하거나, 미리보기 없이 저장
- CI가 실나이스 계정으로 테스트

저장소에 올리는 산출:

- 가명 학생만 있는 픽스처 HTML (`학생01`)
- 역할·라벨 기준 셀렉터 (`button:저장`, 교시 헤더 등). 실명 CSS 경로에 의존하지 않기
- “이 빌드를 교사 PC에서 시운전함. 저장 여부: 예/아니오” 같은 비식별 기록

시운전 순서:

1. 웹앱 대기열 미리보기 확인
2. 나이스는 교사가 직접 로그인·조회
3. 확장은 사용자 제스처(버튼) 후에만 입력
4. 첫 시운전은 저장 전 중단하거나, 이미 같은 내용이 있는 날짜·소수 행만
5. 나이스에 반영된 값을 교사가 확인한 뒤에만 “반영됨”
6. 오입력은 앱이 롤백하지 않음. 나이스에서 교사가 정정

---

## 7. 개발 운영 방식 (봇 + GitHub)

한 채널에서 역할 봇이 협업하고, **실제 코드와 결정은 GitHub에 남깁니다.**

```
기능 제안
  → 담당 봇이 이슈 초안 작성
  → 총괄이 우선순위·범위 확정
  → 개발/디자인/매크로가 구현 브랜치에서 작업
  → 현장검증이 수용기준 확인
  → 보안 관리자가 체크리스트 검토 후 Approve
  → main 병합
```

- 브랜치: `feat/이슈번호-짧은이름`, `fix/`, `sec/`
- 커밋: Conventional Commits (`feat:`, `fix:`, `docs:`, `sec:`)
- 이슈 라벨: `web` `ui` `macro` `security` `docs` `needs-teacher-review`
- `main` 보호: 보안 관리자 리뷰 필수

역할: 총괄 한결, 웹앱 지우, 디자인 서연, 매크로 도윤, 보안 가온, 현장 민재. `docs/BOTS.md`

채널에서는 공지·이슈 쪼개기·짧은 리뷰만. 파일 단위 코드는 해당 봇과 1:1.  
한 턴에 봇을 하나만 부른다. 여섯이 동시에 코드를 쓰면 안 된다.

---

## 8. 기술 제안 (변경 가능)

| 구분 | 제안 | 이유 |
| --- | --- | --- |
| 웹앱 | Vite + React + TypeScript | 봇이 서로 다른 스택을 만들지 않도록 1차로 고정 |
| 로그인 | Google Identity Services + 확장 `chrome.identity` | 비밀번호를 우리 창에서 받지 않음 |
| 상태 | 로컬 IndexedDB (Dexie 등) | 서버 없이 학급 데이터 유지 |
| 양식 | ExcelJS / SheetJS | 교사 엑셀 출석부와 왕복 |
| 확장 | Chrome Manifest V3 | 나이스 탭 DOM 조작 |
| 테스트 | Playwright 비식별 픽스처 + 교사 입회 시운전 | CI는 실계정 없이. 나이스 실화면은 교사 로그인·미리보기 후 육안 확인 |
| 문서 | Markdown + 이슈/PR 템플릿 | 교사도 읽기 쉬운 운영 |

스택은 위 표로 1차 고정한다. 바꾸면 총괄이 이슈로 남긴다.

---

## 9. 면책

- 나이스 UI·업무 규칙은 시도교육청·학년도·패치에 따라 바뀝니다.
- 자동입력이 실패한 채 부분 저장되거나, 규정이 다른 학생(전입 등)에게 잘못 적용될 수 있습니다.
- 사용 전후로 나이스 화면을 반드시 육안 확인하세요.
- 학교·교육청 보안 지침과 충돌하면 사용을 중단하세요.

---

## 10. 라이선스

MIT. 교육 현장 비영리 사용을 전제로 한다. 나이스 자체는 이 라이선스와 무관하다.

---

## 시작하기

1. 저장소를 클론한다.
2. `docs/data-contract.md`와 `SECURITY.md`를 읽는다.
3. `docs/FIRST-ISSUES.md` 순서대로 이슈를 연다. 코드는 이슈 없이 시작하지 않는다.
4. 나이스 셀렉터는 추측하지 않는다. 화면 검토는 6.1.
5. PR은 `.github/PULL_REQUEST_TEMPLATE.md` + 보안 가온 승인 후에만 `main`.

문서 목록:

| 파일 | 내용 |
| --- | --- |
| `README.md` | 제품·MVP·운영 |
| `SECURITY.md` | 보안 기준 (루트) |
| `docs/SECURITY.md` | 같은 보안 기준 |
| `docs/data-contract.md` | 구분·종류·클릭 규칙·JSON |
| `docs/auth.md` | 구글 로그인·계정별 칸 |
| `docs/BOTS.md` | 그록 봇 이름·레이블·설명 |
| `docs/FIRST-ISSUES.md` | 코딩 전 이슈 |
| `docs/issue-template.md` | 이슈 본문 양식 |
| `.github/PULL_REQUEST_TEMPLATE.md` | PR 체크 |
| `.github/ISSUE_TEMPLATE/` | GitHub 이슈 양식 |
| `tests/fixtures/` | 가명 CSV·대기열 샘플 |
| `LICENSE` | MIT |
